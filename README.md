# MineFinds

Private Android Minecraft coordinate journal.

MineFinds stores worlds, seeds, current coordinates, and logged finds locally on the phone. It can sort saved finds by distance and open Chunkbase Seed Map centered on saved coordinates.

## Build

GitHub Actions builds a debug APK on every push to `main` and uploads it as the `MineFinds-APK` artifact.
